{
  "name": "recipe-app",
  "version": "1.0.0",
  "private": true,
  "description": "Personal recipe web app using vanilla JS, Supabase, and SCSS",
  "scripts": {
    "build:css": "sass scss/style.scss dist/style.css",
    "watch:css": "sass --watch scss/style.scss:dist/style.css",
    "test": "node --test tests/"
  },
  "devDependencies": {
    "sass": "^1.93.2"
  }
}
